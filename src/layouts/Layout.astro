---
import '../styles/global.css';
import SeoHead from '../components/SeoHead.astro';
import ZoomerMode from '../components/ZoomerMode';
import SiteLoader from '../components/SiteLoader';
import { getContent } from '../lib/content';
interface Props {
  title?: string;
  description?: string;
  keywords?: string;
  image?: string;
  lang?: string;
  ogLocale?: string;
}
const {
  title = 'Chirkova Dentist — Косметична стоматологія у Дніпрі',
  description = 'Cтоматологія у Дніпрі. Вініри, імпланти, елайнери, відбілювання. Перша консультація безкоштовно.',
  keywords,
  image,
  lang = 'uk',
  ogLocale = 'uk_UA',
} = Astro.props;

const zoomerLang = lang === 'en' ? 'en' : 'uk';
const content = await getContent(zoomerLang);
---
<!DOCTYPE html>
<html lang={lang}>
<head>
  <SeoHead
    title={title}
    description={description}
    keywords={keywords}
    image={image}
    lang={lang}
    ogLocale={ogLocale}
  />
</head>
<body>
  <SiteLoader client:load />
  <slot />
  <ZoomerMode client:load lang={zoomerLang} data={content} />
</body>
</html>
